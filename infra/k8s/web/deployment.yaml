apiVersion: apps/v1
kind: Deployment
metadata:
  name: affable-web
  namespace: affable
spec:
  replicas: 1
  selector:
    matchLabels:
      app: affable-web
  template:
    metadata:
      labels:
        app: affable-web
      namespace: affable
    spec:
      containers:
        - name: affable-web
          image: nginxdemos/nginx-hello:plain-text
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
