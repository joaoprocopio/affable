services:
  mysql:
    image: mysql:9.6
    ports:
      - published: 3306
        target: 3306
    volumes:
      - type: volume
        source: mysql_data
        target: /var/lib/mysql
    environment:
      MYSQL_DATABASE: affable
      MYSQL_ROOT_PASSWORD: root

  beanstalk:
    image: schickling/beanstalkd:latest
    ports:
      - published: 11300
        target: 11300

  # laravel:
  #   build:
  #     context: ./apps/core
  #     dockerfile: dockerfile
  #   working_dir: /var/www
  #   volumes:
  #     - ./apps/core:/var/www
  #   command: ["php-fpm"]

volumes:
  mysql_data:
